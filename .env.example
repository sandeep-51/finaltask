# MongoDB Configuration (Required)
# Get your MongoDB connection string from MongoDB Atlas or your MongoDB provider
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=YourAppName
DATABASE_NAME=event_registration

# Admin Configuration (SECURITY CRITICAL!)
# This password protects your entire admin panel
# Use a strong, unique password - minimum 12 characters recommended
# DO NOT use the default value in production!
ADMIN_PASS=eventadmin@1111

# Site URL for QR code generation and redirects
SITE_URL=http://localhost:5000

# SMTP Email Configuration (For sending QR codes via email)
# RECOMMENDED FOR RENDER: Use SendGrid (Free tier: 100 emails/day)
# Alternative options: Mailgun, AWS SES, or Gmail

# === OPTION 1: SendGrid (RECOMMENDED FOR RENDER) ===
# Sign up at: https://signup.sendgrid.com/
# After signup, create an API key at: https://app.sendgrid.com/settings/api_keys
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key

# === OPTION 2: Gmail (For Development Only) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# Email Sender Information
# IMPORTANT: For SendGrid, verify this email at: https://app.sendgrid.com/settings/sender_auth
EMAIL_FROM=noreply@yourevent.com
EMAIL_FROM_NAME=Event Registration

# Server Port
PORT=5000

# Session Secret (IMPORTANT: generate a random string for production)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-key-change-in-production

# Node Environment (CRITICAL: Set to 'production' when deploying)
NODE_ENV=development
